---
base: ../tkn-bundle/tkn-bundle.yaml
add:
  - use-source
removeWorkspaces:
  - source
preferStepTemplate: true
replacements:
  workspaces.source.path: /var/workdir
regexReplacements:
  "/workspace(/.*)": /var/workdir$1
